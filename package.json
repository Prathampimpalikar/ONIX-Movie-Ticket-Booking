{
  "name": "onix-movie-ticket-booking",
  "version": "1.0.0",
  "private": true,
  "description": "ONIX - State-of-the-Art Full Stack Movie Ticket Booking Application",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "dev": "cd client && npm run dev",
    "preview": "cd client && npm run preview"
  }
}
